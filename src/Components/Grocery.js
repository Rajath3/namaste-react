const Grocery = () => {
    return (
        <div>
            <h1>Let's assume this is a large component and have it's own set of sub components. Now if the bundle size becomes huge. We start using lazy loading/dynamic importing</h1>
        </div>
    )
}

export default Grocery;